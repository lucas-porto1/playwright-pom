{
  "name": "playwright-pom-js",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "test": "playwright test",
    "test:chromium": "playwright test --project=chromium",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "npm run lint && npm run format:check && npm run test:chromium"
  },
  "author": "Lucas Porto",
  "license": "ISC",
  "description": "JavaScript reference architecture for end-to-end QA automation with Playwright and Page Objects.",
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "dotenv": "^17.4.2",
    "eslint": "^10.8.1",
    "eslint-plugin-playwright": "^2.11.0",
    "globals": "^17.9.0",
    "prettier": "^3.9.6"
  }
}
